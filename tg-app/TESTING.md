# TESTING.md — Руководство тестировщика

**Проект:** Telegram Mini App «Промгидравлика — каталог гидроцилиндров»
**Бот:** @PromGidravlika_Bot
**Mini App URL:** https://my-demo-project-nt8u.vercel.app/
**Сервер бота:** https://193.168.48.98:8443 (Beget VPS, PM2)
**Webhook:** https://193.168.48.98:8443/webhook

---

## 1. Архитектура (что тестируем)

```
tg-app/
  index.html              — единственный файл Mini App (весь UI, CSS, JS, данные)
  server.js               — Express HTTPS-сервер (запускается на VPS)
  api/webhook.js          — обработка команд и заявок бота
  api/set-webhook.js      — одноразовый endpoint регистрации webhook
  api/platform-webhook.js — платформа мастеров (онбординг)
  api/m/[slug].js         — страница мастера /m/:slug
  api/master/[slug].js    — API данных мастера (JSON)
  lib/logger.js           — логирование (info/warn/error → logs/errors.log)
  lib/supabase.js         — клиент Supabase
  vercel.json             — настройки Vercel (rewrites, CORS-заголовки)
  .env.example            — шаблон переменных окружения
```

**Стек:** Vanilla HTML/CSS/JS (фронтенд), Node.js ES modules + Express (сервер), Supabase (БД), Telegram Bot API.

Два независимых компонента:
- **Mini App** — фронтенд, работает в браузере или внутри Telegram (Vercel)
- **Бот** — серверная часть на VPS, получает команды и заявки из Mini App

---

## 2. Запуск без Telegram (браузер)

Самый быстрый способ увидеть UI:

```bash
# Клонировать репозиторий
git clone https://github.com/AexeyLihachev018/My-demo-project.git
cd My-demo-project/tg-app

# Просто открыть файл в браузере — зависимостей нет
open index.html          # macOS
start index.html         # Windows
xdg-open index.html      # Linux
```

В браузере `window.Telegram` отсутствует → приложение автоматически переходит в режим `hasTG = false`:
- Данные хранятся в `localStorage` (не в CloudStorage)
- MainButton и BackButton Telegram не работают — кнопки отображаются прямо на странице
- Все экраны и логика работают полноценно

**Сброс состояния для повторного тестирования онбординга/оффера:**

```js
// Открыть DevTools → Console и выполнить:
localStorage.clear();
location.reload();
```

Конкретные ключи localStorage:
| Ключ              | Что хранит                              |
|-------------------|-----------------------------------------|
| `onboard_v1`      | Флаг «онбординг пройден» (значение `1`) |
| `offer_shown_v1`  | Флаг «оффер показан» (значение `1`)     |
| `tma_cart_v2`     | JSON массив позиций в заявке            |
| `tma_recent_v2`   | JSON массив недавно просмотренных (≤10) |

---

## 3. Запуск в Telegram

1. Открыть Telegram → найти **@PromGidravlika_Bot**
2. Нажать **Start** или отправить `/start`
3. Бот ответит приветствием и кнопкой **«📦 Открыть каталог»**
4. Нажать кнопку — откроется Mini App

> Если кнопки нет или бот не отвечает — см. раздел «Диагностика» ниже.

---

## 4. Переменные окружения (для разворачивания копии)

Файл `.env` на VPS: `/app/bot/tg-app/.env` (не в git, создать по шаблону `.env.example`):

```env
BOT_TOKEN=<токен от @BotFather>
APP_URL=https://193.168.48.98:8443
MANAGER_CHAT_ID=<chat_id менеджера — узнать через @userinfobot>
SUPABASE_URL=<Project URL из Supabase>
SUPABASE_SERVICE_ROLE_KEY=<service_role ключ из Supabase>
WEBHOOK_SECRET=<любая строка для защиты /api/set-webhook>
```

После обновления .env перезапустить бота:
```bash
pm2 restart bot
```

Зарегистрировать webhook с самоподписанным сертификатом (один раз):
```bash
curl -F "url=https://193.168.48.98:8443/webhook" \
     -F "certificate=@ssl/server.crt" \
     "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook"
```
Должен вернуть `{"ok":true}`.

Проверить статус webhook:
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
# Ожидаемо: "url" содержит 193.168.48.98:8443/webhook, "last_error_message" отсутствует
```

---

## 5. Тест-кейсы

### 5.1 Бот — команды

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| B-01 | Отправить `/start` | Ответ: «Привет, [имя]!» + описание + кнопка «📦 Открыть каталог» с web_app |
| B-02 | Отправить `/start` без имени (через клиент где нет first_name) | Ответ: «Привет!» (без имени) |
| B-03 | Отправить `/help` | Ответ с разделами Каталог/Калькулятор/Заявка + контакты + кнопка |
| B-04 | Отправить `/catalog` | Ответ: «Открываю каталог 👇» + кнопка |
| B-05 | Отправить `/calc` | Тот же ответ что `/catalog` (обрабатывается одним условием) |
| B-06 | Отправить произвольный текст («привет», «цена», «123») | Ответ: «Используйте кнопку ниже» + кнопка |
| B-07 | Не отправлять ничего (нажать команду из меню) | Аналогично соответствующей команде |

### 5.2 Онбординг (первый запуск)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| O-01 | Открыть приложение при `localStorage` без ключа `onboard_v1` | Показывается экран онбординга, нижнее меню **скрыто** |
| O-02 | На экране онбординга открыть в Telegram с именем пользователя | Показывается «Привет, [first_name]!» |
| O-03 | На экране онбординга открыть в браузере | Показывается «Привет!» (без имени) |
| O-04 | Нажать кнопку **«Начать»** | Переход на главный экран, нижнее меню появляется, вкладка «Подбор» активна |
| O-05 | После нажатия «Начать» перезагрузить страницу | Онбординг больше не показывается (ключ `onboard_v1=1` в localStorage) |

### 5.3 Оффер-модалка

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| F-01 | Первый просмотр после онбординга (нет `offer_shown_v1`) | Через ~320 мс после перехода появляется модалка с анимацией |
| F-02 | Нажать **«Получить скидку 15%»** | Открывается t.me/PromGidravlika_bot/app (в Telegram) или браузере; модалка закрывается |
| F-03 | Нажать **«Пропустить»** | Модалка закрывается с анимацией |
| F-04 | Нажать на затемнённый фон вне карточки | Модалка закрывается |
| F-05 | Закрыть и открыть приложение повторно | Модалка больше не показывается (`offer_shown_v1=1`) |

### 5.4 Нижняя навигация

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| N-01 | На главном экране | Вкладка «Подбор» активна (акцентный цвет) |
| N-02 | Нажать вкладку «Расчет» | Переход на экран калькулятора, вкладка «Расчет» активна |
| N-03 | Нажать вкладку «Каталог» | Переход на каталог **без фильтра** (все товары), вкладка «Каталог» активна |
| N-04 | Из каталога открыть карточку товара | Вкладка «Каталог» остаётся активной |
| N-05 | Нажать вкладку «Спецификация» | Переход на экран заявки, вкладка «Спецификация» активна |
| N-06 | Нажать вкладку «Профиль» | Переход на экран профиля, вкладка «Профиль» активна |
| N-07 | Добавить товар в заявку | На вкладке «Спецификация» появляется красный бейдж с числом (1) |
| N-08 | Добавить ещё один товар | Бейдж показывает (2) |
| N-09 | Удалить все товары из заявки | Бейдж исчезает |
| N-10 | Повторно нажать активную вкладку (например «Подбор» будучи на главном) | Страница прокручивается наверх |
| N-11 | Перейти на вкладку «Каталог» с любого экрана | `S.catFilter` сбрасывается: D=null, Dmax=null, P=null |

### 5.5 Главный экран

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| H-01 | Открыть главный экран | Заголовок «Промгидравлика», строка поиска, 6 плиток категорий (Ø32, Ø40, Ø50, Ø63, Ø80, Ø100+), кнопка «Подобрать по параметрам», кнопка «Поделиться с другом» |
| H-02 | В заявке есть товары | Блок «Ваша заявка» с числом позиций отображается выше категорий |
| H-03 | Просмотрено ≥1 товара | Блок «Недавно смотрели» со скролом по горизонтали (≤6 карточек) |
| H-04 | Нажать на плитку «Ø63» | Переход в каталог с фильтром D=63, Dmax=63 (только Ø63) |
| H-05 | Нажать на плитку «Ø100+» | Переход в каталог с фильтром D=100, Dmax=999 (Ø100 и выше) |
| H-06 | Нажать строку поиска | Переход на экран поиска |
| H-07 | Нажать «Подобрать по параметрам» | Переход на калькулятор, шаг 1 |
| H-08 | Нажать «Поделиться с другом» в Telegram | Открывается шаринг с текстом «Каталог гидроцилиндров Промгидравлика...» и ссылкой https://t.me/PromGidravlika_Bot |
| H-09 | Нажать «Поделиться с другом» в браузере (поддержка navigator.share) | Вызывается системное окно шаринга |
| H-10 | Нажать «Поделиться с другом» в браузере (без navigator.share) | Ссылка копируется в буфер через navigator.clipboard |

### 5.6 Поиск

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| S-01 | Открыть поиск | Поле ввода с автофокусом, пустое состояние «Введите артикул, диаметр или ход» |
| S-02 | Ввести `ГЦ-63` | Найдены 5 позиций (ГЦ-63/40-200, -300, -500, -800, -1000) |
| S-03 | Ввести `63` | Находит товары с «63» в артикуле, D или d — результат ≥5 позиций |
| S-04 | Ввести `300` | Находит все товары с ходом 300 мм (или D/d = 300) |
| S-05 | Ввести `ГЦ-200` | Найдены 2 позиции (ГЦ-200/125-500 и ГЦ-200/125-800) |
| S-06 | Ввести несуществующий артикул (например `ГЦ-999`) | «Ничего не найдено», кнопка «Написать менеджеру» |
| S-07 | Нажать × (крестик) в поле | Запрос очищается, возвращается пустое состояние |
| S-08 | Нажать кнопку «Назад» | Возврат на предыдущий экран |
| S-09 | Из результатов нажать на товар | Переход в карточку, товар добавляется в «Недавно смотрели» |
| S-10 | Нажать «Написать менеджеру» | Открывается https://t.me/PromGidravlika_Bot |

### 5.7 Каталог

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| C-01 | Открыть каталог через плитку «Ø32» | Заголовок «Гильза Ø32 мм», 3 позиции |
| C-02 | Открыть каталог через плитку «Ø100+» | Заголовок «Гильза Ø100–999 мм», позиции от Ø100 до Ø200 |
| C-03 | Нажать чип «16 МПа» | Список фильтруется: только товары с P=16 |
| C-04 | Нажать чип «25 МПа» | Только товары с P=25 |
| C-05 | Нажать чип «Все» | Фильтр снимается, все позиции категории |
| C-06 | Нажать «⚙ Калькулятор» в чипах | Переход на калькулятор |
| C-07 | Нажать на товар | Переход в карточку товара |

**Товары с P=25 МПа:** ГЦ-80/50-800, ГЦ-80/50-1000, ГЦ-100/63-800, ГЦ-125/80-300, ГЦ-125/80-500, ГЦ-160/100-300, ГЦ-160/100-500, ГЦ-200/125-500, ГЦ-200/125-800 (9 позиций).

### 5.8 Карточка товара

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| P-01 | Открыть ГЦ-63/40-300 | SVG-схема цилиндра с подписями D=63, d=40, L=300 |
| P-02 | Проверить параметры | Артикул, D, d, L, P, объём поршн. полости, расход насоса (t=3с), толкающее усилие |
| P-03 | ГЦ-63/40-300: расчётные данные | V_p = π×(0.0315)²×0.3×1000 ≈ 0.935 л; Q ≈ 18.7 л/мин; F_push = 16×10⁶×π×(0.0315)² ≈ 49 856 Н = 49.9 кН |
| P-04 | Проверить бейдж наличия | «В наличии» (зелёный ●), «7–10 дней» (жёлтый ◐), «Под заказ» (серый ○) |
| P-05 | Нажать «+ Добавить в заявку» (браузер) | Кнопка меняется на «✓ В заявке (1) — открыть» |
| P-06 | Повторно нажать «+ Добавить» | Товар не дублируется (cartAdd возвращает false) |
| P-07 | Нажать «Поделиться» | Открывается шаринг с артикулом и ссылкой p-gidravlika.ru |
| P-08 | Нажать «Назад» | Возврат в каталог или поиск (корректный стек) |

**Товары для граничных проверок:**
- Самый дорогой: ГЦ-200/125-800 — 43 500 ₽, «Под заказ»
- Самый дешёвый: ГЦ-32/20-100 — 2 400 ₽, «В наличии»
- Всего позиций в каталоге: **26**

### 5.9 Калькулятор — Шаг 1 (подбор диаметра)

Формула: `D_min = √(4F / πP) × 1000` мм, затем округление вверх по ГОСТ 3822-79.
Ряд ГОСТ: 32, 40, 50, 63, 80, 100, 125, 160, 200, 250, 320.

| # | Входные данные | Ожидаемый результат |
|---|----------------|---------------------|
| K-01 | F=50000 Н, P=16 МПа | D_min ≈ 63.0 мм → ГОСТ **63 мм** |
| K-02 | F=50000 Н, P=25 МПа | D_min ≈ 50.5 мм → ГОСТ **63 мм** |
| K-03 | F=10000 Н, P=16 МПа | D_min ≈ 28.2 мм → ГОСТ **32 мм** |
| K-04 | F=200000 Н, P=16 МПа | D_min ≈ 126.2 мм → ГОСТ **160 мм** |
| K-05 | Переключить единицу на «кг», ввести F=5000 кг, P=16 | Конвертируется: 5000×9.81=49050 Н → D_min ≈ 62.5 → **63 мм** |
| K-06 | Пустое поле F, нажать «Перейти к шагу 2» | Inline-ошибка «Введите усилие» под полем, к шагу 2 не переходит |
| K-07 | P=0 или P>35, нажать кнопку | Inline-ошибка «Введите давление (1–35 МПа)» |
| K-08 | Ввести корректные данные → результат появляется сразу (live) | Блок «Результат» появляется без нажатия кнопки |

### 5.10 Калькулятор — Шаг 2 (полный расчёт)

| # | Входные данные | Ожидаемый результат |
|---|----------------|---------------------|
| K2-01 | D=63, d=40, L=300, t=3 | V_p≈0.935 л, V_s≈0.559 л, Q≈18.7 л/мин, t_ret≈1.79 с, F_push≈49856 Н, F_pull≈29793 Н |
| K2-02 | d ≥ D (например D=50, d=60) | `S.res2 = null`, результат не показывается |
| K2-03 | t=0 (не задано) | Q=null → время обратного хода не считается (функция calcCyl возвращает null) |
| K2-04 | Нажать «← Шаг 1» | Возврат к шагу 1, значения F и P сохранены |
| K2-05 | При наличии результата нажать «Добавить в заявку» (браузер) | Переход на экран заявки; артикул вида «ГЦ-63/40-300»; `fromCalc: true`; цена «По запросу»; срок «7–10 дней» |

### 5.11 Заявка (Спецификация)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| R-01 | Открыть пустую заявку | Пустое состояние с подсказкой |
| R-02 | Добавить товар, открыть заявку | Позиция с артикулом, параметрами, кнопка удаления (✕) |
| R-03 | Нажать ✕ рядом с позицией | Позиция удаляется, бейдж на вкладке обновляется |
| R-04 | Добавить комментарий в поле | Текст сохраняется (видно при отправке) |
| R-05 | Нажать «Отправить менеджеру» (браузер) | alert с JSON данными: items[], comment, user |
| R-06 | Нажать «Отправить менеджеру» в Telegram (через inline-кнопку бота) | `tg.sendData()` отправляет данные боту, TMA закрывается |
| R-07 | Нажать «Отправить» в Telegram (не через inline-кнопку) | Показывается `tg.showAlert` с объяснением что нужно открыть через @PromGidravlika_Bot |

### 5.12 Получение заявки менеджером (требует MANAGER_CHAT_ID)

| # | Условие | Ожидаемый результат |
|---|---------|---------------------|
| M-01 | `MANAGER_CHAT_ID` задан в Vercel env | После `sendData` менеджер получает сообщение с позициями, именем клиента и временем (Екатеринбург) |
| M-02 | `MANAGER_CHAT_ID` не задан | Менеджер ничего не получает, пользователь всё равно видит подтверждение |
| M-03 | Клиент имеет username | Сообщение менеджеру содержит `@username` |
| M-04 | Клиент без username | Сообщение содержит `tg_id: <число>` |
| M-05 | Позиция добавлена из калькулятора | В строке позиции присутствует `_(расчёт)_` |

### 5.13 Экран «Профиль»

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| Pr-01 | Открыть вкладку «Профиль» в Telegram | Круглый аватар с инициалами (2 буквы), имя и @username |
| Pr-02 | Открыть в браузере | Аватар с «?», имя «Гость» |
| Pr-03 | Пользователь без фамилии | Одна буква в аватаре |
| Pr-04 | Проверить контакты | +7 (3412) 77-57-04, 8 (800) 444-70-65, info@p-gidravlika.ru, p-gidravlika.ru, Пн–Пт 8:30–17:00 |
| Pr-05 | Нажать «@PromGidravlika_Bot» | Открывается чат с ботом |
| Pr-06 | Нажать «Поделиться с коллегой» | Аналогично кнопке «Поделиться с другом» на главном экране |

### 5.14 Навигация и стек экранов

| # | Сценарий | Ожидаемый результат |
|---|----------|---------------------|
| Nav-01 | Главный → Каталог → Карточка → «Назад» | Возврат в каталог (не на главный) |
| Nav-02 | Главный → Каталог → Карточка → вкладка «Подбор» | Переход на главный, стек = `['home']`, кнопка «Назад» скрыта |
| Nav-03 | Стек >1 в Telegram | BackButton Telegram виден |
| Nav-04 | Стек = 1 в Telegram | BackButton скрыт |
| Nav-05 | BackButton нажат когда стек = 1 | Вызывается `tg.close()` (TMA закрывается) |
| Nav-06 | Скролл вниз → нажать «Назад» → вернуться | Позиция скролла восстанавливается (S.scrollPos) |
| Nav-07 | Поиск → ввести текст → «Назад» → снова открыть поиск | Поле очищено (S.searchQ = '' при переходе go-search) |

---

## 6. Тёмная тема

Все CSS-переменные берутся из Telegram SDK (`--tg-theme-*`). В браузере переключается через системные настройки.

**Проверить в браузере:** DevTools → Rendering → «Emulate CSS media feature prefers-color-scheme: dark»

Критично проверить:
- Бейджи наличия: `.b-ok` меняет цвет с `#1e8a3a` на `#30d158`, `.b-soon` с `#b36b00` на `#ff9f0a`
- Разделитель `.sep` меняется с `rgba(0,0,0,0.1)` на `rgba(255,255,255,0.1)`
- Все фоны и тексты через `var(--bg)`, `var(--text)`, `var(--hint)` — адаптируются автоматически

---

## 7. Граничные случаи для проверки

| Случай | Что проверить |
|--------|---------------|
| Каталог через вкладку «Каталог» | Отображаются все 26 товаров без фильтра |
| Поиск по `200` | Находит ГЦ-200/125-* и товары где d или D = 200 |
| Добавить в заявку один товар дважды | В заявке остаётся 1 запись (дублей нет) |
| Калькулятор: d = D (шток = гильза) | Результат не рассчитывается, блок результата не появляется |
| localStorage недоступен (приватный режим) | try/catch не бросает, приложение работает без сохранения |
| Очень длинное имя пользователя Telegram | Аватар берёт первые 2 символа — проверить что текст не вылезает |
| Список «Недавно смотрели»: открыть >10 товаров | Сохраняются последние 10, отображаются последние 6 |

---

## 8. Диагностика: бот не отвечает

**Шаг 1.** Проверить статус процесса PM2:
```bash
ssh root@193.168.48.98
pm2 status
# Должно быть: online
```

**Шаг 2.** Посмотреть логи ошибок:
```bash
pm2 logs bot          # логи в реальном времени
cat /app/bot/tg-app/logs/errors.log   # файл ошибок
```

**Шаг 3.** Проверить статус webhook:
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```
Должно быть: `"url": "https://193.168.48.98:8443/webhook"`.

**Шаг 4.** Отправить тестовый POST вручную:
```bash
curl -k -X POST https://193.168.48.98:8443/webhook \
  -H "Content-Type: application/json" \
  -d '{"message":{"chat":{"id":123},"text":"/start","from":{"first_name":"Test"}}}'
# Ожидаемо: {"ok":true} или {"ok":false,"error":"..."} с деталями
```

**Шаг 5.** Зарегистрировать webhook заново:
```bash
cd /app/bot/tg-app
curl -F "url=https://193.168.48.98:8443/webhook" \
     -F "certificate=@ssl/server.crt" \
     "https://api.telegram.org/bot$(grep BOT_TOKEN .env | cut -d= -f2)/setWebhook"
```

---

## 9. Как сообщать о баге

**Минимум в описании:**
1. Среда: браузер (какой) / Telegram (iOS / Android / Desktop)
2. Шаги для воспроизведения
3. Ожидаемый результат
4. Фактический результат + скриншот
5. Состояние localStorage (если баг в онбординге/оффере/корзине): `JSON.stringify(localStorage)` из консоли

**Для багов бота:**
- Время отправки команды (UTC)
- Какую команду отправили
- Что ответил бот (или что не ответил)
- Логи из Vercel Functions (если есть доступ)

---

## 10. Что НЕ тестировать

- `My-demo-project/vizitka.html` — личная визитка, не является частью Mini App, правила проекта на неё не распространяются
- `api/set-webhook.js` — одноразовый endpoint, в продакшне уже зарегистрирован; лишний вызов безвреден
- Адаптивность на десктопе — приложение предназначено для мобильных (≥320px), десктопный вид не является целевым
